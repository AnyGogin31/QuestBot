[project]
name = "quest-bot"
version = "1.0.0"
requires-python = ">=3.14"

dependencies = [
    "aiogram >= 3.26.0",
    "aiosqlite >= 0.22.1",
    "alembic >= 1.18.4",
    "pydantic-settings >= 2.13.1",
]

[project.optional-dependencies]
linux-only = [
    "uvloop >= 0.22.1",
]
